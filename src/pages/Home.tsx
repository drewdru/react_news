import React from "react";

import { Main } from "components";

const Home: React.FC = () => {
  return (
    <div className="d-flex flex-column min-vh-100">
      <Main />
    </div>
  );
};

export default Home;
